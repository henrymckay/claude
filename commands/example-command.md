---
description: Template/example slash command. Replace with what this command does.
argument-hint: "[optional args]"
---

This is the prompt that runs when you type `/example-command`.

Arguments are available as `$ARGUMENTS` (all) or `$1`, `$2`, … (positional).

Reach for a **skill** instead if you want something reusable that Claude can
auto-invoke; use a command for a simple, explicitly-invoked prompt template.
