---
name: example-skill
description: Template/example skill. Replace this with a specific one-line summary of WHAT the skill does and WHEN Claude should use it — include concrete trigger words, e.g. "Use when the user asks to ...".
---

# Example Skill

This is a starter template. Delete it or copy it to a new folder when you write
your first real skill.

## When to use

Describe the situations this skill applies to. Claude reads the `description`
frontmatter (not this body) to decide whether to auto-invoke, so make that line
count. This section is for the detailed "how".

## Steps

1. Step one — be explicit and imperative.
2. Step two — reference any supporting files by relative path, e.g. `scripts/run.sh`.
3. Step three.

## Conventions

- Keep instructions concrete and ordered.
- Put long reference material in separate files in this folder and point to them,
  rather than bloating this file.
